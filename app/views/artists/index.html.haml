%h2 Artists:
= link_to 'New Artist', new_artist_path
%ol
  - @artists.each do |artist|
    %li
      = link_to artist.name, artist_path(artist)
      = link_to 'Edit', edit_artist_path(artist)
      = link_to 'Delete', artist_path(artist), :confirm => 'Are you sure?', :method => :delete
